# Add these lines to your Laravel backend .env file
# Location: C:\CAPSTONE PROJECT\campus-nav\.env

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password-here
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@navistfind.com
MAIL_FROM_NAME="${APP_NAME}"

# Instructions:
# 1. For Gmail:
#    - Enable 2-factor authentication
#    - Go to: https://myaccount.google.com/apppasswords
#    - Generate an "App Password" for "Mail"
#    - Use that app password in MAIL_PASSWORD (not your regular password)
#
# 2. For testing (Mailtrap):
#    MAIL_MAILER=smtp
#    MAIL_HOST=sandbox.smtp.mailtrap.io
#    MAIL_PORT=2525
#    MAIL_USERNAME=your-mailtrap-username
#    MAIL_PASSWORD=your-mailtrap-password
#    MAIL_ENCRYPTION=tls
#
# 3. After updating .env, run:
#    php artisan config:clear









